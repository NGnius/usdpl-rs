[workspace]
members = [
    "usdpl-core",
    "usdpl-front",
    "usdpl-back",
    "usdpl-build",
]

exclude = [
    "templates/decky/backend"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
debug = false
strip = true
lto = true
codegen-units = 4
